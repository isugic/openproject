# /api/v3/work_packages/{work_package_id}/reminders/{reminder_id}
---
patch:
  summary: Update a work package reminder
  operationId: update_reminder
  tags:
    - Work Packages
    - Reminders
  description: |-
    Updates an existing reminder for the specified work package.

    A user can only update their own active reminder.

    **Required permission:** view work packages for the project the work package is contained in.
  parameters:
    - name: work_package_id
      in: path
      description: Work package id
      example: '1'
      required: true
      schema:
        type: integer
    - name: reminder_id
      in: path
      description: Reminder id
      example: '1'
      required: true
      schema:
        type: integer
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            remindAt:
              type: string
              format: date-time
              description: The date and time when the reminder is due
            note:
              type: string
              description: The note of the reminder (optional)
  responses:
    '200':
      description: Reminder updated successfully
      content:
        application/hal+json:
          schema:
            $ref: '../components/schemas/reminder_model.yml'
    '404':
      description: |-
        Returned if the work package does not exist or the client does not have sufficient permissions to see it.
      content:
        application/hal+json:
          schema:
            $ref: '../components/schemas/error_response.yml'

delete:
  summary: Delete a work package reminder
  operationId: delete_reminder
  tags:
    - Work Packages
    - Reminders
  description: |-
    Deletes an existing reminder for the specified work package.

    A user can only delete their own active reminder.
  parameters:
    - name: work_package_id
      in: path
      description: Work package id
      example: '1'
      required: true
      schema:
        type: integer
    - name: reminder_id
      in: path
      description: Reminder id
      example: '1'
      required: true
      schema:
        type: integer
  responses:
    '204':
      description: Reminder deleted successfully
    '404':
      description: |-
        Returned if the work package does not exist or the client does not have sufficient permissions to see it.
      content:
        application/hal+json:
          schema:
            $ref: '../components/schemas/error_response.yml'
